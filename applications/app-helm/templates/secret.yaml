{{- if .Values.secret.create -}}
kind: Secret
apiVersion: v1
metadata:
  name: example-secret-test1
type: Opaque
stringData:
  username: {{ .Values.secret.username }}
  password: {{ .Values.secret.password }}
{{- end }}
